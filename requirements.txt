# ── Core ──────────────────────────────────────
customtkinter>=5.2.0
psutil>=5.9.0
pyinstaller>=6.0.0

# ── System Tray ────────────────────────────────
pystray>=0.19.0
Pillow>=10.0.0

# ── Températures ──────────────────────────────
# Windows uniquement
wmi>=1.5.1; sys_platform == "win32"

# macOS uniquement
# osx-cpu-temp  → pip install osx-cpu-temp
# rumps         → pip install rumps  (requis pour le system tray sur macOS)